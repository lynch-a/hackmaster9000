<div id="accordion">
  <div class="card">
    <div class="card-header">
      <h5 class="mb-0">
        <button class="btn btn-link" id="<%= tool_name%>-help-heading" data-toggle="collapse" data-target="#<%= tool_name %>-help" aria-expanded="false" aria-controls="<%= tool_name %>-help">
          Crt.sh Help
        </button>
      </h5>
    </div>

    <div id="<%= tool_name %>-help" class="collapse" data-toggle="collapse" aria-labelledby="<%= tool_name %>-help-heading" data-parent="#accordion">
      <div class="card-body bg-light">
        <pre style="margin-bottom:0;">Crt.sh is a service that aggregates https certification data. It is useful for discovering subdomains without brute forcing them.

Given a target like: %.tesla.com

This tool will curl crt.sh with ?q=%.tesla.com&type=json and then the scanloader will parse it and load the domains into the "Domains" tab.
</pre>
      </div>
    </div>
  </div>
</div>
<hr>

<div class="container no-gutters">
  <form id="<%= tool_name %>-options">
    <div class="row">
      <div class='col-sm-6'>    
        <%= erb :'tools/tool_helpers/_text_input', locals: {
                  tool_name: tool_name,
                  label: "Target",
                  field_name: "target",
                  field_value: "",
                  placeholder: "%.tesla.com"
                }
        %>
      </div>
      
      <div class='col-sm-6'>

      </div>
    </div>
  </form>
  <hr>
  <%= erb :'partials/_tool_runner', layout: false, locals: {tool_name: "crtsh"} %>
</div>
